generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model ownedCoins {
  id       String @id @default(auto()) @map("_id") @db.ObjectId
  Amount   Int
  Price    Int
  Total    Int
  CoinName String
}
